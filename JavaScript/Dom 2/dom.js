var countdown=document.getElementById('countdown')

initialValue=countdown.innerHTML

var backGround=document.getElementById('bg-image')

// var quit = setInterval(function(){

//     initialValue=initialValue > 0 ? initialValue-1 : 0;

//     countdown.innerHTML=initialValue

//     countdown.style.fontSize=initialValue > 0 ? initialValue*100+"px" : "100px";

//     backGround.style.width = initialValue > 0 ? initialValue*10+"%" : "10%"


// },1000)